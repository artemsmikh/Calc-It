<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CalculatorButtonRow">
        <item name="android:layout_weight">1</item>
        <item name="android:weightSum">4</item>
    </style>

    <style name="CalculatorButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/button_calculator_dark</item>
        <item name="android:layout_margin">@dimen/calculator_button_margin</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/colorFont</item>
        <item name="android:padding">0dp</item>
    </style>

    <style name="CalculatorButtonLight" parent="CalculatorButton">
        <item name="android:background">@drawable/button_calculator_light</item>
    </style>

    <style name="CalculatorButtonAccent" parent="CalculatorButton">
        <item name="android:background">@drawable/button_calculator_accent</item>
    </style>

</resources>
